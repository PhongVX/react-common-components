import React from 'react';
import Edit from '../../assets/Edit.svg';

const EditIcon = (props) => {
    return (
        <img
            src={Edit}
            {...props}
        />
    )
}

export default EditIcon